.env
.env.*
!.env.example
!.env.local.example
node_modules/
.next/
dist/
build/
.DS_Store
Thumbs.db

# Local agent/tooling scratch. Harmless if you use none of it.
scratchpad/
.claude/settings.local.json

# Safety-net runtime state, written by the hooks in .claude/. Generated for you;
# never commit it. The hooks resolve these from .claude/ itself (see
# .claude/hooks/shared.mjs), so the paths are exact, not a guess.
.claude/.session-state.json
.claude/.hook-errors.log
.claude/.naysayer-result.json

# The /naysayer review skill writes its verdict at the repo ROOT, which is a
# different path from the hooks' copy above. Both are tool state, neither is
# template content.
.naysayer-result.json

# Project-specific
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
/out/
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# Supabase CLI scratch
supabase/.temp/
