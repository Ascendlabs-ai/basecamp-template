# Provisioning the database

Five SQL files, applied in order, then one INSERT to create the first
administrator. Nothing else. **Only the third is optional** — it is starter
categories and nothing more; the other four build the schema, lock it, and open
the two write paths the admin screens use.

| File | What it is |
|---|---|
| `migrations/0001_baseline.sql` | The whole schema: tables, enums, functions, policies, triggers, grants. Generated by `pg_dump --schema-only --no-owner`; contains **no rows**. |
| `migrations/0002_security_boundary.sql` | Hand-written. Pins ownership, hardens the definer functions, and **asserts** the boundary rather than describing it. |
| `migrations/0003_seed_categories.sql` | Hand-written, and **optional**. Four starter categories — Sales, Marketing, Operations, Useful Tools — so a new app has somewhere to put its first entry. Contains no schema at all. Idempotent, and safe to skip entirely. |
| `migrations/0004_admin_write_paths.sql` | Hand-written, and **required**. Opens the trust root's INSERT/DELETE so administrators can promote each other from the app, adds `basecamp.log_privileged_action()`, widens `list_people()` with ban state and member type, and seeds the three starter member types **Add person** needs in order to offer anything. Without it **Add person** and the roster's ⋮ menu do not work. Idempotent, and it asserts its own post-conditions the way `0002` does. |
| `migrations/0005_category_nesting.sql` | Hand-written, and **required** — not optional. The home page, the catalog admin and the access matrix all `select` `categories.parent_id`, so without this migration all three fail with `42703 column does not exist`. Adds `categories.parent_id`, an ON DELETE RESTRICT self-reference, and `enforce_category_depth()` — the trigger that caps nesting at one level in both directions. Adds no grant and no policy: `authenticated` already held all four verbs on `categories` from 0001, so nesting is decided by the policies that were already there. Idempotent, atomic, and asserts its own post-conditions. |
| `tests/` | Not applied to your database. `boundary_mutations.sh` is the proof that `0002`'s assertions actually catch things, and `_supabase_surface_stub.sql` is what lets a bare PostgreSQL cluster stand in for Supabase while it runs. Optional; see step 1. |

> **On the filenames.** These are numbered `0001`/`0002` rather than carrying the
> Supabase CLI's 14-digit timestamps, because they are applied once at
> provisioning through the SQL Editor or `psql` — not pushed with
> `supabase db push`. If you would rather manage them with the CLI, rename them
> to timestamped form first.

> **Where these files come from.** `0001` is a `pg_dump` squash of a private
> upstream application; regenerating it is that project's job, not yours, and is
> documented in [`MAINTAINING.md`](../MAINTAINING.md). Two headers in `0001`
> record its provenance — `SOURCE-MIGRATION-VERSION` (which upstream migration it
> was squashed from) and `GENERATED-ON` (the date). **Both are provenance only:
> nothing in this repository verifies them**, so treat the date as "how old this
> schema is", not as a guarantee it is current.

---

## Order of operations

### 0. Expose the `basecamp` schema to the Data API — do this first

Supabase Dashboard → **Integrations → Data API → Settings → Exposed schemas**
(on older projects this setting is under **Project Settings → API**). Add
`basecamp` alongside whatever is already listed.

This is a project setting, not schema DDL, so `pg_dump` cannot carry it and no
migration can set it. Skip it and PostgREST answers `PGRST106` for every
request — the app renders an error and nothing in the database looks wrong,
which is a genuinely difficult failure to diagnose from the outside.

The field is replaced wholesale when edited. **Add** `basecamp`; never remove an
existing entry.

### 1. Apply the files, in order

Paste them into the SQL Editor, which stops at the first error on its own — or,
from a terminal, **use these flags and not a bare `-f`**:

```bash
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 --single-transaction -f supabase/migrations/0001_baseline.sql
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f supabase/migrations/0002_security_boundary.sql
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f supabase/migrations/0003_seed_categories.sql
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f supabase/migrations/0004_admin_write_paths.sql
psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f supabase/migrations/0005_category_nesting.sql
```

**`0003` is the optional one; `0004` is not.** `0003` adds four starter categories and
nothing else — no table, no policy, no grant — so it cannot affect anything
`0002` checks. It goes *after* `0002` rather than before because `0002` is the
gate that says the schema is sound, and there is no point seeding a database you
have not yet confirmed. Skip it if you would rather start from an empty catalog;
you can create categories in **Admin → Catalog** either way, and you can apply it
later. It is idempotent, so re-running it changes nothing — but note that it
matches on `slug`, so a category you *deleted* comes back if you run it again,
while one you *renamed* is left alone.

`psql` without `ON_ERROR_STOP=1` prints an error and **keeps going**, leaving a
half-created schema that looks installed. That is not hypothetical: an earlier
version of `0001` used a PG17-only privilege, and on PG16 four `GRANT`s failed
while the run reported success. `0002` now refuses in that situation (it counts
what must exist and checks the grants the app needs), but the flags are still
the difference between one clear failure and a database you have to inspect.

`0002` ends by asserting the security boundary. If it commits without raising,
each of the following was checked and held on your database:

- every `basecamp` table is owned by `postgres`, has RLS on, and does not have
  FORCE RLS (which would make the definer helpers recurse against their own
  policies);
- every SECURITY DEFINER function is `postgres`-owned, has `search_path` pinned
  empty, and is not executable by `PUBLIC`;
- `anon` holds nothing anywhere in the schema, and no policy names `anon` or
  `public`;
- `authenticated` *can* reach the schema and read `basecamp.entries` — the
  check that catches an install which would otherwise fail
  `permission denied for schema basecamp` on every request;
- `authenticated` holds neither **UPDATE nor TRUNCATE** on
  `basecamp.super_admins`, and neither does `service_role` — an UPDATE would
  reassign the roster without changing the row count, so the last-row guard
  would never fire. **INSERT and DELETE are expected after `0004`** and are not
  a finding: `0004` grants them so the roster can be administered from the app,
  and the policies that were always in `0001` are what decide who may use them.
  `0002` checks the policies as well as the privileges, precisely because the
  privilege alone no longer tells you anything;
- every guard trigger exists, is **attached to the table it is supposed to
  watch**, and is enabled **for ordinary traffic** — all three, because the
  weaker forms were defeated: a disabled trigger keeps its definition, a trigger
  left `ENABLE REPLICA` fires only for replication traffic and never for your
  app, and counting names without the table let a same-named
  decoy elsewhere hold the count while the real guard was gone;
- the audit table is genuinely append-only: `authenticated` holds SELECT and
  nothing else, its mutation and TRUNCATE guards are enabled, and all four audit
  writers are attached to the tables they are supposed to watch;
- **no definer TRIGGER function is executable by anyone but its owner.** This is
  the one that reads like housekeeping and is not: holding EXECUTE on one of
  them is enough for an ordinary signed-in role to get that function to run with
  its owner's rights and write rows into the append-only audit log *as
  `postgres`* — while a direct INSERT from the same session is refused.
  `CREATE TRIGGER` exercises EXECUTE, and that privilege check is the only thing
  in the way. Nothing legitimate
  needs the grant: PostgreSQL refuses to call a trigger function directly, and
  the trigger machinery never consults EXECUTE. **Do not re-grant it to get
  something working;**
- **the seven functions that decide access still decide it** — `is_super_admin`,
  `has_grant`, `category_has_grant`, `can_read_entry`, `can_read_category`,
  `log_access_change` and `list_people` are pinned by md5 of their bodies. Not a
  substring test: a body of `select true` followed by a comment naming the right
  tables satisfies every substring check and makes every caller an administrator.
  `list_people` is on the list because it is the one that returns other people's
  email addresses, and a body keeping `from auth.users` while dropping only its
  admin check passes every weaker test there is. These seven ship identically to
  every stamp of this template, so an exact pin is right for them — see below for
  what happens when you change one on purpose.

  **Two more are pinned the same way but checked only once they exist**, because
  the migrations that create them run after `0002` does on a fresh stamp:
  `log_privileged_action` (from `0004`, the audit writer for account-lifecycle
  events) and `category_or_child_has_grant` (from `0005`, which is the entire
  non-admin arm of the categories SELECT policy). The second is why the list is
  worth re-reading when you add a migration: it arrived checked for existence and
  for being a definer, and neither of those stops a body of `select true` from
  handing every category to somebody with no grants. Nine bodies are pinned in
  total once the chain is fully applied;

  `list_people` is the one exception to "identically": `0004` replaces its body
  to add ban state and member type, and `0002` runs both before and after that.
  So its expected digest is **selected** by whether `0004` has been applied —
  one digest for any given database, never a set of two, because a set would let
  a *revert* to the older body pass. `0004` pins the audit writer it ships,
  `log_privileged_action`, the same way;
- **every policy consults the access model**, and the obvious permit-alls are
  refused. This one is a floor rather than an
  equality, and it is the weakest check in the file in **both** directions: it
  misses some predicates that consult the access model and still admit everyone,
  and it wrongly
  refuses an own-row policy you add on your own table. Read
  [`issues.md`](../issues.md) → "Known gaps in the security boundary" before you
  add a policy;
- no principal outside `postgres` / `authenticated` / `service_role` holds a
  function, column or schema privilege in `basecamp`, and no materialized view
  exists — a matview cannot carry `security_invoker` at all, so it would be an
  unconditional RLS bypass;
- the schema-wide **default** privilege for future tables grants `service_role`
  no TRUNCATE — asserted against `pg_default_acl` directly, because a list of
  today's tables can only ever name today's;
- no view in `basecamp` runs with owner rights — since PG15 a view without
  `security_invoker` reads straight past RLS;
- **the schema is actually complete** — the expected tables, policies, functions,
  triggers and both default-ACL rows are present, and `authenticated` holds the
  positive grants the app needs. Everything above this line is a *negative* fact
  ("`anon` holds nothing"), and negative facts pass on a schema that was never
  fully created.

None of that is a description of intent. Each item is an assertion inside `0002`
that raises and rolls the whole file back if it does not hold.

**And you can check that for yourself rather than taking it on trust.** The
assertions are mutation-tested: `supabase/tests/boundary_mutations.sh` breaks one
thing at a time in a throwaway PostgreSQL 16 or 17 cluster and requires the file
under test to refuse. You do not need it to provision; run it if you edit
`0002`, `0004`, `0005`, a policy or a definer function — or if you just want the
proof. Its header says how to start a scratch cluster.

**Deliberately four arms, counted separately.** The script prints a total per arm
and asserts each count against the cases it actually ran, because one rolled-up
number is where a lost arm hides — which is not hypothetical: the Editor-path arm
once drifted to a shorter migration chain than the others and went on printing a
plausible total while proving less.

| Arm | What it asks |
|---|---|
| Static, `psql` | break one thing in a mirror, require `0002` to refuse |
| Static, **Editor path** | the same, with the migrations pasted CRLF and whole-file, the way a client applies them |
| Runtime | issue real statements as `authenticated` and require the **database** to refuse |
| `0004` under test | break something *before* `0004` runs, and require `0004` to refuse |

A handful of cases expect a **commit** and print `PASS [COMMITTED]`; that is
correct, not a failure. Some are mutations `0002` *repairs* — it assigns
ownership and EXECUTE before it asserts, which is what makes a fresh install
self-correcting. One adds a policy that still consults the access model, a
legitimate extension. One adds a definer in `public` that never names
`basecamp`, which is nothing to do with this schema. The rest are controls on an
unbroken schema, including one that applies the whole chain through the Editor
path — a file that refused everything would prove nothing either.

**What a green run does not mean.** It says the assertions catch the mutations
that are IN the file. A review on 2026-08-17 got past `0002` with further
mutations that are *not* — each proven on live PostgreSQL 16 and 17 — so read
[`issues.md`](../issues.md) → "Known gaps in the security boundary" alongside
this list. None is reachable by an ordinary signed-in user of your app, and a
service-role API key does not reach them either.

If you deliberately change one of the pinned function bodies, `0002` will
refuse until you re-derive its digest — which is the point, because it forces
someone to read the new body first:

```sql
select proname,
       md5(replace(replace(prosrc, chr(13)||chr(10), chr(10)), chr(13), chr(10)))
  from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'basecamp' and proname = '<fn>';
```

Run that in the Supabase SQL Editor, against the project you changed the body in.
The `replace(...)` pair is not decoration and must not be dropped: `0002` hashes
the same normalized text, because pasting the migrations from a clipboard or a
checkout that carries CRLF stores carriage returns in the body that the
template's own file does not have. Re-deriving a digest from raw `prosrc` on such
a database bakes that one machine's line endings into the pin, and the template
then refuses every clean install afterwards — including yours, on the next
project you stamp.

### 1b. Set the Auth URL configuration

Dashboard → **Authentication → URL Configuration**:

- **Site URL** → your deployed origin (e.g. `https://apps.example.com`).
- **Redirect URLs** → must include **both** of these, and both again on
  `http://localhost:3000` if you run the app locally:
  - `https://your-origin/accept-invite`
  - `https://your-origin/auth/confirm`

These are the two halves of the onboarding flow. **Add person** creates the
account and generates a link to `/accept-invite`, where the person chooses their
first password; **Issue a sign-in link** generates one to `/auth/confirm`, which
verifies a single-use token and drops them straight into the app. Nothing is
emailed either way — the link is displayed once, for you to pass on yourself —
but Supabase still generates it against this allow-list, so an origin that is
not on it comes back pointing somewhere else.

**Get this wrong and Supabase does not error.** It silently substitutes the Site
URL, which on a fresh project is `http://localhost:3000`, so the link you hand
somebody points at a machine they do not have. Set this before you add your
first person — and if a link ever looks like it is aimed at the wrong host, this
is the setting, not a bug in the app.

### 2. Create the administrator's account

Dashboard → **Authentication → Users → Add user**. This is the *only* account
you create by hand: everyone after this one is added from `/admin/access` →
**Add person**, which is what steps 1b and 5 exist to make work.

**Note what "no self-signup" does and does not mean here.** This app ships no
signup screen, so nobody signs up *through it* — but the Supabase project
underneath has signup **open by default** (`disable_signup: false`), and that is
what the roster assumes: people can create an account on your project and arrive
with **zero access**, and an administrator grants them what they need. The
sign-in page's copy about accounts being issued by an administrator is true of
the app, not of the project.

If you want that copy to be literally true, turn signup off in
**Authentication → Providers → Email** and create every account by hand.

### 3. Insert their trust-root row

There is **no bootstrap function**. This is a deliberate, attributed INSERT:

```sql
insert into basecamp.super_admins (user_id, note)
select u.id, 'first administrator, provisioned at install'
  from auth.users u
 where u.email = 'PUT-THE-ADMIN-EMAIL-HERE'
on conflict (user_id) do nothing;
```

Run it in the SQL Editor, which connects as `postgres` — so RLS does not apply
and no policy can refuse it.

**Why not an RPC that promotes whoever calls it first?** Because that is a
security decision with no operator in the loop and no attribution: the row would
record "the first signed-in account claimed it", not who decided or why. It also
saves exactly one statement in a flow that already requires the dashboard twice.
The cost of doing it this way is real — it is a hand-edited SQL statement in
every install — which is why step 4 exists.

### 4. Verify before you trust it

`cp .env.local.example .env.local`, fill in the values — the two required ones,
plus `SUPABASE_SERVICE_ROLE_KEY` if you want **Add person** to work — then
`npm run dev`, sign in as that account, and confirm **`/admin/access` renders
the administration screen** rather than the locked panel.

The install is not complete until you have seen that. The locked panel is what
the screen looks like when the database says you are not an administrator — it
is a consequence of the policy answer, not a separate check, so seeing the real
screen is the only proof the trust root took.

### 5. Build your catalog

Categories and entries are `super_admin`-only writes, enforced by policy. Sign
in and go to **Admin → Catalog**:

- **Entries** — "Add an app" takes a name, a URL and a category; everything else
  gets a default you can change. "All fields" opens the same form with nothing
  hidden.
- **Categories** — create, rename, reorder and delete, and nest one level deep
  with the "Sits under" picker. A category holding entries *or subcategories*
  cannot be deleted; the database refuses it and the screen says so. The
  one-level cap is enforced by a trigger in `0005`, not by the form.

You never type a slug: the identifier each row needs is derived from the name
and then stays put, so renaming later is safe.

If you would rather start with a fuller example, **`supabase/seed.example.sql`**
has 5 categories — one of them nested under another — and 13 entries covering
all three entry types and a spread of statuses, with the non-obvious constraints
(the slug regex, the launchable-needs-a-URL rule, the nav_group restriction)
written out at the top.
Run it, click around, then delete the rows and write your own. It is idempotent.
It is a *worked example* and is unrelated to `0003`, which seeds four empty
categories and nothing else.

Note that you see the catalog only because you are a super_admin. A colleague
with no grants sees nothing until you grant them something in `/admin/access` —
that is the access model working.

**One consequence, and it is not the one an earlier version of this file
described.** An empty category renders nowhere on the home screen — **including
for you**. `src/lib/catalog.ts` drops every category holding no entries:

```ts
const visible = (rows ?? []).filter((c) => (c.entries?.length ?? 0) > 0);
```

That function takes rows and nothing else — it has no viewer, no role, and so
no way to make an exception for an administrator. The database half is the
opposite and is often mistaken for the whole story: `basecamp_categories_select_granted`
is `is_super_admin() OR category_has_grant(id)`, so a super_admin genuinely
*reads* all four seeded rows. The app then filters them out before rendering.
Readable and not shown are different things.

So on a brand-new install, seeded or not, **your first home screen is the "Your
catalog is empty" panel**, which is correct and is what that panel exists to
say. The four categories are real and are waiting in **Admin → Catalog**, which
queries them directly and shows each with its entry count. Put one entry in a
category and it appears on the home screen.

### 6. Add your first person, and confirm they can get in

This is the step the whole provisioning run is for, and it is the one that
proves `0004` applied, the redirect URLs from step 1b are right, and
`SUPABASE_SERVICE_ROLE_KEY` is set. **No dashboard round-trip: you should not
have to create another human by hand after step 2.**

`/admin/access` → **Add person**:

1. Enter their email and pick their **member type**. `0004` seeds three — Staff,
   Contractor, Client — so the picker is never empty on a fresh install. Rename
   them on the **Types** tab if the labels do not suit you; they cannot be
   deleted, because a project with no types cannot add anybody.
2. The app creates the account and shows a **one-time sign-in link**. It is
   displayed once and never stored — it is a credential, so it is not written to
   the audit log, a server log, or an error message. Copy it before you close the
   dialog.
3. Send it to them however you already talk to them. They land on
   `/accept-invite`, choose a password, and are signed in.

Then grant them something — an entry, a category, or a grant on their type — and
have them confirm they see it. **Somebody with a type but no grants sees an empty
catalog, and that is the access model working**, not a failure of this step.

If the link points at `http://localhost:3000` when you did not expect it, that is
step 1b, not a bug. If **Add person** says the service-role key is missing, that
is step 4.

Afterwards, each person's ⋮ menu offers **Issue a sign-in link** for anyone
locked out, **Make / remove an administrator**, and **Suspend / restore
sign-in**. Suspension touches no grants, which is what makes restoring somebody
return them to exactly what they had.

---

## Email: not required, and not used for onboarding

**This template ships no email provider.** No SendGrid, no Mailgun, no Postmark,
no Resend, no nodemailer, no SMTP credentials and no sending domain — there is
no mail dependency in `package.json` at all.

**Onboarding does not need one.** `/admin/access` → **Add person** creates the
account and shows the administrator a one-time sign-in link to hand over
directly; the same is available for anyone locked out, from a person's ⋮ menu.
Nothing is sent, so nothing can silently fail to arrive. **If that is the only
way you add people, you can skip this whole section.**

**Nothing in this app sends mail at all.** There is no forgot-password form and
no recovery-email button; earlier versions had both, and they were removed
rather than kept alongside the issued link — `README.md` → "What is deliberately
not here" records why, and what it costs.

So why is this section here? Because **Supabase itself will still send mail on
your project** — a confirmation message when somebody signs up, for one, since
signup is open by default. The limits below are what you get if you have not
attached your own SMTP, and they are the reason this template does not build
anything that depends on delivery. They are also what you would be taking on if
you added a mail-based flow of your own.

The built-in service is a development convenience, not a mail provider, and it
is limited in two ways that together make it unusable for reaching real people:

| Limit | Effect |
|---|---|
| **~2 emails per hour, project-wide** | Not per address, not per admin — per project. Sending to a third person within the hour fails. |
| **Team members' addresses only** | It will only deliver to email addresses belonging to members of your Supabase organisation. Mail to anyone else is silently not delivered. |

**And the failure is silent in both directions.** Supabase's recovery endpoint
returns success whether or not anything was delivered — by design, so it cannot
be used to discover which addresses exist — so any flow built on it reports
success to the operator while the recipient receives nothing.

**The administrator-issued link has none of these limits**, because it sends no
mail: no rate limit, no organisation-member restriction, and no silent failure.
The link either appears on the administrator's screen or the screen says why it
did not. If someone cannot get in, issue them one from the roster.

### Configure your own SMTP only if you add a mail-based flow yourself

Nothing here requires it. If you build something that does — your own
forgot-password form, a notification, anything — do these two together, because
each fails silently on its own:

Dashboard → **Project Settings → Authentication → SMTP Settings** → enable
**Custom SMTP**, then supply your own provider's host, port, username, password,
and a sender address on a domain you control. Any provider works. Nothing in
this codebase names one, and nothing needs to change here when you pick.

- **Raise the rate limit.** Authentication → Rate Limits → *Emails sent per
  hour*. The default of 2 stays in force even after you attach your own SMTP —
  custom SMTP does **not** lift it for you.
- **Verify your sending domain** with your provider (SPF/DKIM), or the mail
  lands in spam, which looks identical to "it never arrived".

---

## The audit log

`basecamp.access_audit` records access changes — who, for whom, what, when — and
is visible to administrators at **Admin → Audit**.

It covers the four surfaces that decide what a person can see: `access_grants`
(individual grants), `type_grants` (grants to a member type), `members` (which
type a person holds — a type change is recorded as a revoke of the old plus a
grant of the new, and moving a member row to a *different person* is recorded
the same way), and `super_admins` (the trust root). UPDATE on the two grant
tables was removed rather than audited, so a grant can only be added or removed.

**The database writes it, not the app.** AFTER triggers on those four tables
insert the rows, so a grant and its audit record commit together or neither
does. That is forced by this app's architecture rather than chosen for elegance:
every write comes from a browser under RLS, so an audit row the *client* was
responsible for inserting would record exactly the changes that chose to be
recorded. It also means changes made outside the app — a grant issued in the SQL
Editor, an administrator added by hand — appear too, with the actor shown as
`System` because there is no signed-in user.

**Append-only is enforced, not declared.** No INSERT/UPDATE/DELETE policy;
`authenticated` holds SELECT only; and a `BEFORE UPDATE OR DELETE` trigger
raises unconditionally, which stops the table *owner* as well — owners bypass
RLS but not triggers. TRUNCATE has its own guard, for the same reason the trust
root does. To prune deliberately, disable the guard inside a transaction,
delete, re-enable; `0002` fails if you leave it disabled.

No foreign keys, and labels snapshotted at write time: an audit row must survive
deletion of everything it names. With an FK, deleting an app or an account would
silently delete the evidence that access to it was ever granted.

> **Known gap, measured rather than hedged.** The design intends labels to be
> snapshotted, but `log_access_change()` is an `AFTER` trigger that resolves them
> by *lookup* — so on a **cascade** delete the parent row is already gone and the
> label is **never written at all**. Confirmed by execution on two paths a client
> will actually take:
>
> - deleting an account in the Supabase dashboard (`access_grants.user_id →
>   auth.users ON DELETE CASCADE`) writes revoke rows with `subject_label` NULL —
>   *what* was revoked is recorded, *from whom* is not;
> - the ordinary **Delete type** button does the same to `type_grants`.
>
> The audit row survives, which is the point of having no foreign keys, and
> `subject_id` is still recorded — but there is no longer anything to resolve it
> against. Direct DELETEs and every in-app grant/revoke are unaffected and label
> correctly. Closing this properly means snapshotting the labels onto the grant
> tables at write time; it is not fixed here.

---

## Administering the roster afterwards

**Do this from the app.** `/admin/access` → a person's ⋮ menu → **Make an
administrator** / **Remove administrator**. `0004_admin_write_paths.sql` grants
`authenticated` the INSERT and DELETE that the policies in `0001` were always
written for, so the roster is self-administering: the write goes straight from
your browser on your own token and the INSERT policy's `WITH CHECK` gates on the
*caller*, so a non-administrator cannot promote themselves whatever the
interface believes. There is no API route for this on purpose — a role check in
TypeScript would not be a second lock. `supabase/tests/boundary_mutations.sh`
proves it by trying, under a real session.

The SQL below is the fallback for the two cases the screen cannot serve: before
`0004` is applied, and if you have locked yourself out entirely (see the last
section).

Add an administrator:

```sql
insert into basecamp.super_admins (user_id, note)
select u.id, 'promoted by <you>, <date>, <reason>'
  from auth.users u
 where u.email = 'THEIR-EMAIL-HERE'
on conflict (user_id) do nothing;
```

Remove one:

```sql
delete from basecamp.super_admins where user_id = 'THEIR-UUID-HERE';
```

Two floors protect the table, and both refuse rather than warn:

- **The last row cannot be deleted.** A `BEFORE DELETE` row trigger raises, and
  the exception aborts the whole statement — so even
  `delete from basecamp.super_admins` leaves *every* row intact, not one.
  Promote a replacement before demoting the last administrator.
- **`TRUNCATE` is refused outright.** A `BEFORE TRUNCATE` statement trigger
  raises unconditionally, because `BEFORE DELETE` triggers do not fire on
  TRUNCATE — the delete guard alone would have left the table emptiable in one
  word.

If a dashboard user-deletion fails with
`23503 … basecamp_super_admins_user_id_fkey`, that is the foreign key refusing
on purpose: the account is an administrator. Demote them first.

---

## If you lock yourself out

Every admin capability is gated by `basecamp.is_super_admin()`, and all access
is RLS on the signed-in person's own token. The one privileged path — the three
account-lifecycle routes that use `SUPABASE_SERVICE_ROLE_KEY` — is **not an
escape hatch**: it asks Postgres `basecamp.is_super_admin()` on the caller's own
token first and answers 403 before anything privileged is constructed, and even
then it can only reach Supabase Auth, never a `basecamp` table. So if that
function stops returning true for everyone, there is **no way back in from
inside the app**, service-role key or not.

The way back in is the SQL Editor, which runs as `postgres`:

```sql
-- Who is currently an administrator?
select u.email, s.note, s.created_at
  from basecamp.super_admins s
  join auth.users u on u.id = s.user_id
 order by u.email;

-- Restore one by email.
insert into basecamp.super_admins (user_id, note)
select u.id, 'lockout recovery'
  from auth.users u
 where u.email = 'PUT-THE-ADMIN-EMAIL-HERE'
on conflict (user_id) do nothing;

-- What does the gate actually read?
select prosrc from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'basecamp' and p.proname = 'is_super_admin';
```

Calling `basecamp.is_super_admin()` bare in the editor proves nothing — it runs
as `postgres`, `auth.uid()` is null, and it returns false for reasons unrelated
to whether your administrators can get in. To ask the question *as* someone,
highlight both of these and run them together:

```sql
select set_config('request.jwt.claims',
  '{"sub":"THEIR-UUID-HERE","role":"authenticated"}', true);
select basecamp.is_super_admin() as must_be_true;   -- last, so you can see it
```

Two details that matter. Put the `is_super_admin()` call **last**: the SQL
Editor renders only the final statement's result grid, so a trailing reset
statement would hide the very answer you are after. And pass `true` as the third
argument — a multi-statement submission runs as one implicit transaction, so a
transaction-local setting both works and reverts itself on commit. `false` would
leave a spoofed identity on a pooled backend after the transaction ends.

This proves the gate's LOGIC while connected as `postgres`. It does not prove
that the `authenticated` role can reach the schema — the exposure step and a
real sign-in are what prove that.
